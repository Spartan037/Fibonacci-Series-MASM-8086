DATA SEGMENT
	FIB DB 00H, 01H
DATA ENDS

CODE SEGMENT
	ASSUME CS:CODE ,DS:DATA
START: 
	MOV AX,DATA
	MOV DS,AX
	LEA SI,FIB
	MOV CX, 09H

LOOP1: 
	MOV AX,[SI]
	MOV BX,[SI+1]
	ADD AX,BX
	INC SI
	INC SI
	MOV [SI],AX
	DEC SI
	DEC CX
	JNZ LOOP1
	HLT
CODE ENDS
END START
